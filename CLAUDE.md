# CLAUDE.md

이 파일은 로컬 작업용 짧은 라우터다.

실제 프로젝트 규칙은 `.aiassistant/rules/README.md`와 그 문서가 연결하는 루트 `docs/` 아래에 있다.

작업 규칙:

- 규칙의 정본은 `docs/`에서 찾는다.
- 이 파일에는 요약만 두고 세부 규칙은 중복하지 않는다.
- 구현 전에 관련 문서를 먼저 읽는다.

읽기 경로:

1. `.aiassistant/rules/README.md`
2. `docs/README.md`
3. 작업별 세부 문서

커밋 메시지 규칙은 `docs/rules/GIT.md` 정본만 따른다.
